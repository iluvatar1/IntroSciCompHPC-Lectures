# Anaconda env file
# Use as: conda env create -f environment.yml
# Then activate as : conda activate herrcomp
name: introscicomphpc
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - anaconda
  - pip
  - pip:
    - -r requirements.txt
